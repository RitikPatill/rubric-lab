{
  "private": true,
  "packageManager": "pnpm@9.15.0",
  "workspaces": [
    "apps/web",
    "packages/shared"
  ],
  "scripts": {
    "dev:web": "pnpm --filter @rubriclab/web dev",
    "lint": "pnpm -r lint",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "prettier": "^3.5.0",
    "eslint": "^9.0.0"
  }
}
